[package]
name = "wasm_module"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasmlanche = { path = "../../../../../hyper/x/contracts/wasmlanche", default-features = false, features = [] }
borsh = { version = "1.5", default-features = false, features = ["derive"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
wee_alloc = "0.4.5"
